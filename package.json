{
  "name": "weather-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --repo https://github.com/mak48/web_project_2.git"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^4.5.10"
  }
}
